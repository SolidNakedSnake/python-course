# Module  01: Basics of Input & Output

This is my first practice folder. I'm learning Python from scratch at home (and realy enjoying it), to build a strong foundation for AI Engineering.

### What I practiced here:
* Using `print()` to show data in the terminal.
* Customizing strings with `sep` (separators) and `end` (line ends).
* Getting text from users with `input()` and saving it into variables.

The script simulates a very basic manual data entry for a future dataset.